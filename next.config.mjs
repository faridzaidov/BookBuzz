/** @type {import('next').NextConfig} */
const nextConfig = {
    //add config for get image from external source
    images: {
        domains: ['images.unsplash.com'],
    },





};

export default nextConfig;
